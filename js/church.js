/* ============================================================
   ★★★ 우리 교회 정보 — 여기만 고치면 사이트 전체가 바뀝니다 ★★★
   ------------------------------------------------------------
   이 파일 하나가 홈페이지의 "이름표"입니다.
   교회명·주소·전화·계좌·목사님 성함 등을 아래에서 바꾸면
   헤더·푸터·제목·미리보기·헌금창까지 사이트 전체에 자동 반영됩니다.

   ※ 외부 서비스(로그인·게시판·푸시 등) 열쇠는 js/config.js 에 있습니다.
     (그 파일도 반드시 우리 교회 것으로 바꿔야 합니다 → 배포안내.md 참고)
   ============================================================ */
window.CHURCH = {
  /* --- 교회 이름 --- */
  name:   "노진교회",           // 한글 정식 명칭
  nameEn: "NOJIN CHURCH",       // 영문 명칭 (푸터·제목에 표시)
  since:  "",                    // 설립 연도, 숫자만 (예: "1964"). 없으면 "" 로 두세요.

  /* --- 소개 문구 --- */
  // 홈 상단·검색 미리보기 등에 쓰이는 한 줄 소개
  tagline: "성경을 따라, 은혜로 살아가는 믿음의 공동체입니다.",

  /* --- 주소·연락처 --- */
  address: "경기도 화성시 만세구 장안면 화곡로 159-8 (18585)",   // 전체 주소
  phone:   "010-0000-0000",         // 대표 전화 (하이픈 포함)

  /* --- 섬기는 분들 (푸터에 표시) ---
     필요한 만큼 줄을 추가/삭제하세요. 순서대로 표시됩니다. */
  pastors: [
    { role: "담임목사", name: "손병민" },
    // { role: "원로목사", name: "○○○" },
    // { role: "협동목사", name: "○○○" },
  ],

  /* --- 온라인 헌금 계좌 (푸터 '온라인헌금' 버튼) ---
     쓰지 않으려면 account 를 "" 로 두세요(헌금 버튼이 숨겨집니다). */
  give: {
    bank:    "",                     // 은행명 (예: "농협")
    account: "",                     // 계좌번호 (하이픈 포함, 화면 표시용)
    holder:  "노진교회",            // 예금주
    // 토스 송금 링크용 계좌번호 — 숫자만(하이픈 없이). 비우면 '토스로 송금' 버튼이 숨겨집니다.
    tossDigits: "",
  },

  /* --- 카카오톡 채널 (푸터 '채널 추가' 버튼) ---
     채널 관리자센터의 채널 홈 URL. 없으면 "" 로 두면 버튼이 숨겨집니다. */
  kakaoChannel: "",

  /* --- 사이트 주소·테마 --- */
  domain:     "https://example.com/",  // 실제 도메인 (끝에 / 포함). 검색·SNS 미리보기에 사용.
  themeColor: "#223350",               // 대표 색(남색). 모바일 주소창·아이콘 배경 등에 사용.

  /* --- 검색엔진 소유확인 (선택) ---
     구글/네이버 서치어드바이저에서 발급받은 인증코드. 없으면 "" 로 두세요. */
  seo: {
    google:   "",
    naver:    "",
    keywords: "노진교회, 화성 교회, 장안면 교회, 예장합동",  // 검색 키워드(쉼표로 구분)
  },
};

/* ------------------------------------------------------------
   [참고] 아래 값은 위 설정에서 자동으로 계산됩니다. 건드리지 마세요.
   ------------------------------------------------------------ */
(function () {
  var c = window.CHURCH;
  // 화면 표시용 영문 부제: "OOO CHURCH · SINCE 1964"
  c.nameEnLine = c.nameEn + (c.since ? " · SINCE " + c.since : "");
  // 브라우저 탭 제목 기본형: "우리교회 | OOO CHURCH"
  c.titleSuffix = c.name + (c.nameEn ? " | " + c.nameEn : "");
})();
